{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  // NOTE: All sensitive values must be set via environment variables or hosting secrets.
}
